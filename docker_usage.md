docker ps [-a|-q]

docker run [options] image_name exec_command
    -i, stdin
    -t, terminal
    -d, run in background
    -name,
    -rm

docker stop container_id
docker start container_id
docker images
docker rmi
docker build
docker pull
docker push
Dockerfile